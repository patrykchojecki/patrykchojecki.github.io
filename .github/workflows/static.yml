name: Build and deploy to GitHub Pages

on:
  push:
    branches: ["master"]
  schedule:
    # Refresh the OpenXBL snapshot every six hours.
    - cron: "17 */6 * * *"
  workflow_dispatch:

permissions: {}

# Only one deployment at a time; don't cancel an in-progress run.
concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pages: write
    env:
      BUNDLE_WITHOUT: development
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: npm

      - name: Build JavaScript assets
        run: |
          npm ci
          npm run build:js

      - name: Test OpenXBL data mapping
        run: npm test

      - name: Fetch Xbox activity
        # Optional personal activity must not block publishing the website.
        continue-on-error: true
        run: node scripts/fetch-openxbl.mjs
        env:
          OPENXBL_API_KEY: ${{ secrets.OPENXBL_API_KEY }}

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4.4'
          bundler-cache: true

      - name: Configure GitHub Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Build Jekyll site
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        # defaults to uploading _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 10
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
